void main() {
  /**
   For funtion that contain just one expession, you can use a shorthand syntax/Arrow Notation.
   => this notation is sometime referred to as arrow syntax.
  don,t use return type
   */
  print(add(5, 6));
}

int add(var a, var b) => a + b;
